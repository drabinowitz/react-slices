jest.dontMock('../slices-method-register');

describe('slices-method-register', function() {
  beforeEach(function() {
    this.register = require('../slices-method-register');
  });

  it('should register key function pairs', function() {
  });

  it('should register nested key function pairs', function() {
  });

  it('should unregister arrays of keys', function() {
  });
});
